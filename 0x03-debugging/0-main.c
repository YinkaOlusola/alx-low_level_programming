#include "main.h"
#include <string.h>

/**
* 0-main - tests function that prints if integer is positive or negative
* Return: 0
*/

int main(void)
{
        int i;

        i = 0;

        result = strcmp(positive_or_negative(i), '0 is zero');

	if (result == 0)
	{
		positive_or_negative(i);
	}
	else
	{
		result;
	}

	

        return (0);
}
